---

import Layout from '../layouts/Layout.astro';

import './styles/global.css';

import Footer from '../components/Footer.astro';

import Header from '../components/Header.astro'

const pageTitle = "Обо мне";

const identity = {
  firstName: "Лина",
  country: "Россия",
  occupation: "Искатель",
};

const skills = ["HTML", "CSS", "JavaScript", "Astro", "Написание документации"];

const happy = true;
const skillColor = "navy";
---

<Layout>
  <html>
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width" />
      <title>{pageTitle}</title>
    </head>
    <body>
      <Header />
      <hr class="wavy-line">

      <main class="about-card">
        <h1>Информация о сайте</h1>
        <h2>... и авторе!</h2>
        <p>Данный сайт был реализован с помощью фреймворка <strong>Astro</strong>.</p>
         <h3>Технологии, реализованные на сайте:</h3>
        <ul>
          <li><strong>Astro</strong> — современный фреймворк</li>
          <li><strong>HTML, CSS, JS</strong> — ручная вёрстка без шаблонов</li>
          <li><strong>Компоненты</strong> и <strong>Layout</strong> — для повторного использования</li>
          <li><strong>Темная/светлая тема</strong> — через CSS-переменные и media queries</li>
          <li><strong>Netlify</strong> — хостинг и автодеплой</li>
        </ul>

        <p>Несколько фактов обо мне:</p>
        <ul>
          <li>Меня зовут {identity.firstName}.</li>
          <li>Моя страна — {identity.country}, а род занятий — {identity.occupation}.</li>
        </ul>

        <p>Мои навыки:</p>
        <ul>
          {skills.map(skill => <li class="skill">{skill}</li>)}
        </ul>

        {happy && <p>Я была рада видеть вас на данной странице!</p>}

        <blockquote>
          Все фрактальные изображения созданы вручную в&nbsp;<a href="https://ya.ru/schdvrm" target="_blank">Шедеврум от Яндекса</a>.
        </blockquote>

       
      </main>
      <hr class="wavy-line">
      <Footer />
    </body>
  </html>

  <style define:vars={{ skillColor }}>
    .about-card {
      max-width: 850px;
      margin: 3rem auto;
      padding: 2rem;
      border-radius: 1rem;
      background-color: var(--card-bg);
      color: inherit;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
      transition: background-color 0.3s ease;
    }

    h1 {
      color: var(--accent-color, purple);
      font-size: 3rem;
      margin-bottom: 0.5rem;
    }

    h2 {
      color: var(--text-color);
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .skill {
      font-weight: bold;
      color: var(--skill-color, navy);
    }

    blockquote {
      margin: 1.5rem 0;
      padding: 1rem 1.5rem;
      background: var(--blockquote-bg, #f4eeff);
      border-left: 4px solid var(--accent-color, #a678f5);
      border-radius: 0.5rem;
      font-style: italic;
      color: var(--blockquote-text, #4b3966);
    }

    a {
      color: var(--accent-color, purple);
    }

    a:hover {
      text-decoration: underline;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --card-bg: #1e1e1e;
        --text-color: #f0f0f0;
        --skill-color: #90c6ff;
        --blockquote-bg: #2d2046;
        --blockquote-text: #d2bdfd;
      }
    }

    @media (prefers-color-scheme: light) {
      :root {
        --card-bg: #ffffff;
        --text-color: #222;
        --skill-color: navy;
        --blockquote-bg: #f4eeff;
        --blockquote-text: #4b3966;
      }
    }
  </style>
</Layout>
